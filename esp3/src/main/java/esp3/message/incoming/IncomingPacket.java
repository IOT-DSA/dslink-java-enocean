package esp3.message.incoming;

import esp3.message.Packet;
import com.serotonin.messaging2.IncomingMessage;

public class IncomingPacket extends Packet implements IncomingMessage {
    // no op
}
